#include <stdio.h>
int main(){
    int arr[5] = {10,20,30,40,50};
    int x ,i,found = 0;

    printf("enter number:");
    scanf("%d",&x);

    for (i=0;i<5;i++){
      if(arr[i] == x){
       printf("found at index%d\n",i);
       found = 1;
        break;
      }
    }
    if (!found){
      printf("Not found\n");
    }
      return 0;
 }
